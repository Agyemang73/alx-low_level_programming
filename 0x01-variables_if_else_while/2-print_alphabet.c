#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int 1 = 'a';
	while (1 <= 'z')
	{
		putchar(1);
		l += 1;
	}
	putchar('\n');
	return (0);
}
